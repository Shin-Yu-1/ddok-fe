import styles from './IntroPage.module.scss';

const IntroPage = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>IntroPage</h1>
    </div>
  );
};

export default IntroPage;
