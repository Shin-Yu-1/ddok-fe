import styles from './UserProfilePage.module.scss';

const UserProfilePage = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>UserProfilePage</h1>
    </div>
  );
};

export default UserProfilePage;
