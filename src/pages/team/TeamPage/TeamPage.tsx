import styles from './TeamPage.module.scss';

const TeamPage = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>TeamPage</h1>
    </div>
  );
};

export default TeamPage;
