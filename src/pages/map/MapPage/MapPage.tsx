import styles from './MapPage.module.scss';

const MapPage = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>MapPage</h1>
    </div>
  );
};

export default MapPage;
