import styles from './ResetPasswordPage.module.scss';

const ResetPasswordPage = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>ResetPasswordPage</h1>
    </div>
  );
};

export default ResetPasswordPage;
